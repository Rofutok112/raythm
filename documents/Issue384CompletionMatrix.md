# Issue 384 Completion Matrix

Issue completion is tracked against code structure and verification, not only
documentation.

| Requirement | Current status | Evidence |
| --- | --- | --- |
| Feature / Scene / Domain / Infrastructure boundaries are documented | Covered | `documents/ArchitectureBoundaries.md` |
| Major features use State / Controller / View(Renderer) / Service vocabulary | Covered with legacy filenames | Play, Editor, Song Select, Title, and Online Catalog now have controller/service boundaries. Some Online Catalog files still use the historical `online_download_*` prefix, but the async owner is `online_catalog::data_controller`. |
| Scenes are thin lifecycle/input/controller/render/navigation entries | Covered for Issue #384 scope | `song_select_scene` async work moved out. Title feature polling moved through `title_hub_update_coordinator`; local song-select update moved to `title_local_song_select_controller`; startup flow moved to `title_startup_controller`; Title draw composition moved to `title_hub_view`; Editor timing/scroll actions moved to `editor_timing_action_controller`; Editor draw composition moved to `editor_screen_view`, UI command application moved to `editor_screen_controller`, and timeline preview/cursor presentation moved to `editor_timeline_screen_controller`; Online Catalog async is controller-owned and preview scrub input is in `online_download_preview_controller`; Play MV loading/render-context setup is in `play_mv_controller`; Song Create form/tags/timing/save/saved views are split across dedicated panels/services/controllers. Main Scene files are below 1000 lines. |
| Views/renderers draw and return commands, without state mutation or side effects | Covered for touched surfaces | Local song select update moved from `seamless_song_select_view` into `title_local_song_select_controller`; `title_hub_view` returns login/profile commands to the Scene; Online Catalog catalog/page/owned/download/ranking futures moved out of view state and preview playback input moved out of the update monolith; Editor DAW/right panel scroll commands now route through `editor_timing_edit_service`; `editor_screen_view` draws the editor screen while `editor_screen_controller` applies the UI command results outside `editor_scene::draw()`. |
| Song Select double structure is organized by feature purpose | Covered | Shared `song_select::state` and `song_select::data_controller` now serve standalone and Title paths. Title-specific upload/scoring/transfer concerns remain in Title controllers. |
| `song_select::state` is organized by catalog / selection / filter / scroll / preview / ranking / dialog / auth | Covered with compatibility facade | Storage split into substructures while legacy field refs keep call sites stable. |
| Catalog / ranking / upload / download / auth async work moves into controllers/services | Covered | Song Select catalog/ranking are in `song_select::data_controller`; Online Catalog catalog/page/chart/owned/download/ranking futures are in `online_catalog::data_controller`; auth/profile/upload are controller-owned. Jacket texture loading remains in the cache object because it is render resource ownership, not catalog state. |
| Gameplay pure logic and external I/O boundaries are distinct | Covered | Timing naming improved. `lane_input_tracker` now contains pure lane event/state tracking under gameplay, while `input_handler` remains the raylib/Windows adapter. `ranking_service` moved from `src/gameplay` to `src/services` so the HTTP/SQLite ranking boundary no longer lives in the Gameplay Domain directory. |
| Play and Editor timing vocabulary is unified | Covered | Play uses chart/audio/visual time names. Editor timing and scroll edits are routed through `editor_timing_edit_service`, which keeps tick/bar-beat vocabulary explicit at the Feature boundary. |
| Song Create persistence and timing import are outside Scene | Covered | `song_create_service` validates form data, copies audio/jacket output, and writes `song.json`; `song_create_midi_importer` parses MIDI tempo/meter events; `song_create_timing_service` owns base timing event insertion, sorting, validation, bar:beat parsing, meter map creation, and metronome tick stepping; `song_create_timing_controller` owns timing event add/delete/apply/flush/import/validation mutation; `song_create_form_panel`, `song_create_tag_editor`, `song_create_timing_panel`, and `song_create_saved_view` own the corresponding UI surfaces. |
| Editor operations are command/service/controller sized for future undo/redo | Covered | Added `editor_note_edit_service`; note add/delete/paste/duplicate/modify and timeline note mutations now flow through service operations while `editor_state` remains the command-history owner. Scroll automation add/modify/delete/guides/curve edits route through `editor_timing_edit_service`. |
| Smoke/controller-level tests are added or updated | Covered | Added `song_select_data_controller_smoke`, `lane_input_tracker_smoke`, `editor_note_edit_service_smoke`, and `editor_timing_edit_service_smoke`; updated Play timing smoke names. |
| Existing related smoke tests pass | Covered | See PR verification notes for the current full related smoke pass. |
